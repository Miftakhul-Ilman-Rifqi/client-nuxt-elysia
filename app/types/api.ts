export interface RuntimeInfo {
    runtime: string;
    version: string;
    platform: string;
    timestamp: string;
}

export interface PingInfo {
    message: string;
    runtime: string;
    timestamp: string;
}
